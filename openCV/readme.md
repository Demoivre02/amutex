readme_content = """# Object Detection Script

This script performs object detection using the MobileNet SSD model.

## How to Run

1. Ensure you have Python installed.
2. Install the required dependencies:
   ```bash
   pip install opencv-python